const createUser = require('../app/controllers/createUser');
const { testing } = require('../config/db');

describe('Test user creation', () => {
    beforeEach(()=>{
        jest.clearAllMocks();
    })
    describe('test create user', () => {
        testing('', () => {
            
        })
    })
})
